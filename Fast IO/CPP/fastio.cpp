#include <bits/stdc++.h>
using namespace std;
int main(){
    // to achieve similar speed of scanf and printf with cin and cout
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string str;
    cin >> str;
    // use '\n' instead of endl
    cout << str << "\n";
    return 0;
}