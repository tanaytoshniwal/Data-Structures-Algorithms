#include <bits/stdc++.h>
using namespace std;
class MultiStack{
    private:
    int capacity;
    int *arr;
    int *stack_size;
};
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    
    return 0;
}